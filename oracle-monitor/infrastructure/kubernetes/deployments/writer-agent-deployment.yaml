apiVersion: apps/v1
kind: Deployment
metadata:
  name: writer-agent-deployment
  namespace: oracle-monitor
  labels:
    app: writer-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: writer-agent
  template:
    metadata:
      labels:
        app: writer-agent
    spec:
      containers:
      - name: writer-agent
        image: oracle-monitor-agent:latest
        command: ["python", "-m", "agents.writer_agent.main"]
        envFrom:
        - configMapRef:
            name: agent-config
        - secretRef:
            name: api-keys-secret
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
