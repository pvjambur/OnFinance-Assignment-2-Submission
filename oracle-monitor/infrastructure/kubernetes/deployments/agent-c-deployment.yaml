apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-c-deployment
  namespace: oracle-monitor
  labels:
    app: agent-c
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-c
  template:
    metadata:
      labels:
        app: agent-c
    spec:
      containers:
      - name: agent-c
        image: oracle-monitor-agent:latest
        command: ["python", "-m", "agents.agent_c.main"]
        envFrom:
        - configMapRef:
            name: agent-config
        - secretRef:
            name: api-keys-secret
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
